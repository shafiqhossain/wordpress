window.addEventListener('DOMContentLoaded', function () {
	window.$j = window.$j || jQuery;

	// $j(document).on('click', '.markets-list .market-item-heading', function () {
	// 	var $marketList = $j(this).parents('.markets-list');
	// 	var isOpen = $j(this).hasClass('active');

	// 	$marketList.find('.market-item-heading').removeClass('active');
	// 	$marketList.find('.market-item-content').slideUp();

	// 	if (!isOpen) {
	// 		$j(this).addClass('active');
	// 		$j(this).parents('.market-item').find('.market-item-content').slideDown();
	// 	}
	// });
});
